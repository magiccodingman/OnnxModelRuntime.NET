# Test asset notice

`mul_1.onnx` is the tiny ONNX Runtime test model from `microsoft/onnxruntime`, path `onnxruntime/test/testdata/mul_1.onnx`.

It is included only as a test/smoke asset and is not packed into the `OnnxModelRuntime.NET` NuGet package. ONNX Runtime is distributed under the MIT License; see the upstream project for its license and notices.
